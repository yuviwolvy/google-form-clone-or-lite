const TextArea = () => {
  return (
    <textarea
      placeholder="Long answer text"
      className="bg-transparent w-full border-b-2"
      rows={1}
    ></textarea>
  );
};

export default TextArea;
